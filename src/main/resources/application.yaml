server:
  port: 8089

spring:
  application:
    name: job-search
  profiles:
    default: dev
  datasource:
    url: jdbc:h2:./db/test;AUTO_SERVER=TRUE
    username: root
    password: qwerty
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
